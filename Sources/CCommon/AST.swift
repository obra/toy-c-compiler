// Placeholder: AST types will be defined in Task 5.
// For now, provide a minimal `Decl` type so other modules compile.
public struct Decl {
    public let name: String
    public init(name: String) { self.name = name }
}
